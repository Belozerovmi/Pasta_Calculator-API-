

========== D:\All Files\Pasta_Calculator-API--main\Pasta_Calculator-API--main\index.html ==========


<!doctype html>
<html lang="ru">
  <head>
    <link rel="stylesheet" href="style.css" />
    <link rel="icon" href="favicon.ico" type="image/x-icon" />
    <link rel="manifest" href="/manifest.json" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    />
    <!-- <link rel="apple-touch-icon" sizes="180x180" href="favicon.ico" />
    <link rel="icon" type="image/png" sizes="192x192" href="favicon.ico" />
    <link rel="icon" type="image/png" sizes="512x512" href="favicon.ico" /> -->
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, viewport-fit=cover, user-scalable=no"
    />
    <title>MacroCalc вЂ” РљР°Р»СЊРєСѓР»СЏС‚РѕСЂ РїРѕСЂС†РёР№</title>
    <script src="https://unpkg.com/html5-qrcode@2.3.8/html5-qrcode.min.js"></script>
  </head>
  <body>
    <div class="container">
      <div class="flex-between" style="margin-bottom: 8px">
        <a href="index.html">
          <img src="food-fusilli-pasta-reginette-svgrepo-com.svg" alt="logo" />
          <h1>MacroCalc</h1>
        </a>
      </div>
      <div class="subhead">Р Р°СЃСЃС‡РёС‚Р°Р№С‚Рµ РІРµСЃ СЃСѓС…РёС… РјР°РєР°СЂРѕРЅ РІ РїРѕСЂС†РёРё</div>

      <div class="category-selector" id="categorySelector">
        <div class="slider-indicator" id="sliderIndicator"></div>
        <button class="category-btn active" data-category="pasta">
          <i class="fas fa-utensils"></i> РњР°РєР°СЂРѕРЅС‹
        </button>
        <button class="category-btn" data-category="rice">
          <i class="fas fa-bowl-rice"></i> Р РёСЃ
        </button>
        <button class="category-btn" data-category="grains">
          <i class="fas fa-seedling"></i> РљСЂСѓРїС‹
        </button>
      </div>
      <!-- РџРѕРґСЃРєР°Р·РєР° РїРѕ СЂР°Р·РІР°СЂРёРІР°РЅРёСЋ -->
      <div id="cookingHint" class="cooking-hint">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          class="icon icon-tabler icons-tabler-outline icon-tabler-info-circle"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path d="M3 12a9 9 0 1 0 18 0a9 9 0 0 0 -18 0" />
          <path d="M12 9h.01" />
          <path d="M11 12h1v4h1" />
        </svg>
        <div class="cooking-hint-text">
          <strong>РџРѕРґСЃРєР°Р·РєР°:</strong>
          <span id="hintText">100 Рі СЃСѓС…РёС… РјР°РєР°СЂРѕРЅ в‰€ 240 Рі РІР°СЂС‘РЅС‹С…</span>
        </div>
      </div>
      <!-- РљР°СЂС‚РѕС‡РєР° РїСЂРѕРґСѓРєС‚Р° -->
      <div class="product-card">
        <div class="product-name-row">
          <span class="product-name" id="productName"
            >РњР°РєР°СЂРѕРЅС‹ (С‚РІС‘СЂРґС‹Рµ СЃРѕСЂС‚Р°)</span
          >
          <button class="edit-link" id="editProductBtn">
            <svg
              class="icon-svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.8"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path
                d="M12 20h9M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"
              />
            </svg>
            <span class="edit-link-text">РР·РјРµРЅРёС‚СЊ</span>
          </button>
        </div>
        <div class="nutrients" id="nutrientsPanel">
          <div class="nutrient">
            <div class="nutrient-value" id="kcalVal">354</div>
            <div class="nutrient-label">РєРєР°Р»</div>
          </div>
          <div class="nutrient">
            <div class="nutrient-value" id="proteinVal">12.0</div>
            <div class="nutrient-label">Р±РµР»РєРё, Рі</div>
          </div>
          <div class="nutrient">
            <div class="nutrient-value" id="fatVal">1.5</div>
            <div class="nutrient-label">Р¶РёСЂС‹, Рі</div>
          </div>
          <div class="nutrient">
            <div class="nutrient-value" id="carbsVal">70.0</div>
            <div class="nutrient-label">СѓРіР»РµРІРѕРґС‹, Рі</div>
          </div>
        </div>
      </div>

      <!-- РџРѕР»СЏ РІРІРѕРґР° -->
      <div class="input-group">
        <label>РЎСѓС…РёРµ РґРѕ РІР°СЂРєРё (РІСЃРµРіРѕ)</label>
        <div class="input-wrapper">
          <input type="number" id="dryTotal" placeholder="200" value="200" />
          <span class="input-unit">Рі</span>
        </div>
      </div>
      <div class="input-group">
        <label>Р“РѕС‚РѕРІС‹Рµ РїРѕСЃР»Рµ РІР°СЂРєРё (РІСЃРµРіРѕ)</label>
        <div class="input-wrapper">
          <input type="number" id="cookedTotal" placeholder="600" value="600" />
          <span class="input-unit">Рі</span>
        </div>
      </div>
      <div class="input-group">
        <label>Р’Р°С€Р° РїРѕСЂС†РёСЏ (РіРѕС‚РѕРІС‹Рµ)</label>
        <div class="input-wrapper">
          <input
            type="number"
            id="portionCooked"
            placeholder="250"
            value="250"
          />
          <span class="input-unit">Рі</span>
        </div>
      </div>

      <button class="btn btn-primary" id="calcBtn">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="1.5"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path stroke="none" d="M0 0h24v24H0z" fill="none" />
          <path
            d="M4 5a2 2 0 0 1 2 -2h12a2 2 0 0 1 2 2v14a2 2 0 0 1 -2 2h-12a2 2 0 0 1 -2 -2l0 -14"
          />
          <path
            d="M8 8a1 1 0 0 1 1 -1h6a1 1 0 0 1 1 1v1a1 1 0 0 1 -1 1h-6a1 1 0 0 1 -1 -1l0 -1"
          />
          <path d="M8 14l0 .01" />
          <path d="M12 14l0 .01" />
          <path d="M16 14l0 .01" />
          <path d="M8 17l0 .01" />
          <path d="M12 17l0 .01" />
          <path d="M16 17l0 .01" />
        </svg>
        Р Р°СЃСЃС‡РёС‚Р°С‚СЊ РїРѕСЂС†РёСЋ
      </button>

      <div class="action-row">
        <button class="btn btn-outline" id="scanBarcodeBtn">
          <svg
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.8"
          >
            <path
              d="M3 7V5a2 2 0 0 1 2-2h2M17 3h2a2 2 0 0 1 2 2v2M21 17v2a2 2 0 0 1-2 2h-2M7 21H5a2 2 0 0 1-2-2v-2"
            />
            <rect x="7" y="7" width="4" height="4" rx="1" />
            <rect x="13" y="7" width="4" height="4" rx="1" />
            <rect x="7" y="13" width="4" height="4" rx="1" />
            <rect x="13" y="13" width="4" height="4" rx="1" />
          </svg>
          РЎРєР°РЅРµСЂ
        </button>
        <button class="btn btn-outline" id="searchProductBtn">
          <svg
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.8"
          >
            <circle cx="10" cy="10" r="7" />
            <path d="M21 21L15 15" />
          </svg>
          РџРѕРёСЃРє
        </button>
      </div>

      <!-- РњРѕРґР°Р»СЊРЅРѕРµ РѕРєРЅРѕ СЂРµР·СѓР»СЊС‚Р°С‚Р° -->
      <div id="resultModal" class="result-modal">
        <div class="result-modal-content">
          <button class="result-modal-close" id="resultModalCloseX">вњ•</button>
          <!-- <div class="result-modal-icon">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.8"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M12 2L12 7" />
              <path d="M12 17L12 22" />
              <path d="M4.71 4.71L8.29 8.29" />
              <path d="M15.71 15.71L19.29 19.29" />
              <path d="M2 12L7 12" />
              <path d="M17 12L22 12" />
              <circle cx="12" cy="12" r="3" />
            </svg>
          </div> -->
          <div class="result-modal-label">Р’ РџРћР Р¦РР</div>
          <div class="result-modal-value">
            <span id="modalDryPortion">--</span>
            <span class="result-modal-unit">Рі</span>
          </div>
          <div class="result-modal-kbju" id="modalKbju"></div>
          <div class="result-modal-actions">
            <button
              id="modalCopyBtn"
              class="result-modal-btn result-modal-btn-primary"
            >
              <svg
                width="18"
                height="18"
                viewBox="0 0 24 24"
                fill="none"
                stroke="white"
                stroke-width="1.8"
                style="display: inline; margin-right: 8px"
              >
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                <path
                  d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"
                ></path>
              </svg>
              РЎРєРѕРїРёСЂРѕРІР°С‚СЊ РІРµСЃ
            </button>
          </div>
        </div>
      </div>
    </div>

    <!-- РњРѕРґР°Р»СЊРЅРѕРµ РѕРєРЅРѕ РїРѕРёСЃРєР°/СЃРєР°РЅРµСЂР° -->
    <div id="modal" class="modal">
      <div class="modal-content">
        <button class="modal-close" id="mainModalCloseX">вњ•</button>
        <h3 id="modalTitle">РџРѕРёСЃРє РїСЂРѕРґСѓРєС‚Р°</h3>
        <input
          type="text"
          id="searchInput"
          class="search-input"
          placeholder="Р’РІРµРґРёС‚Рµ РЅР°Р·РІР°РЅРёРµ РјР°РєР°СЂРѕРЅ РёР»Рё Р±СЂРµРЅРґР°..."
        />
        <div id="searchResults" class="search-results"></div>
        <div id="scannerContainer" style="display: none"></div>
      </div>
    </div>
    <!-- Р¤РЈРўР•Р  -->
    <footer class="footer">
      <div class="footer-content">
        <div class="footer-logo">
          <img
            src="food-fusilli-pasta-reginette-svgrepo-com.svg"
            alt="logo"
            width="24"
            height="24"
          />
          <span>MacroCalc</span>
        </div>
        <div class="footer-links">
          <a
            href="https://github.com/Belozerovmi"
            target="_blank"
            rel="noopener noreferrer"
            class="footer-link"
          >
            <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
              <path
                d="M12 2C6.48 2 2 6.48 2 12c0 4.42 2.87 8.17 6.84 9.49.5.09.68-.21.68-.48 0-.24-.01-.88-.01-1.71-2.78.6-3.37-1.34-3.37-1.34-.46-1.16-1.11-1.47-1.11-1.47-.91-.62.07-.61.07-.61 1.01.07 1.54 1.03 1.54 1.03.89 1.52 2.34 1.08 2.91.83.09-.65.35-1.09.63-1.34-2.22-.25-4.55-1.11-4.55-4.94 0-1.09.39-1.98 1.03-2.68-.1-.25-.45-1.26.1-2.62 0 0 .84-.27 2.75 1.02.8-.22 1.65-.33 2.5-.33.85 0 1.7.11 2.5.33 1.91-1.29 2.75-1.02 2.75-1.02.55 1.36.2 2.37.1 2.62.64.7 1.03 1.59 1.03 2.68 0 3.84-2.34 4.68-4.57 4.93.36.31.68.92.68 1.85 0 1.34-.01 2.42-.01 2.75 0 .27.18.58.69.48C19.13 20.17 22 16.42 22 12c0-5.52-4.48-10-10-10z"
              />
            </svg>
            GitHub
          </a>
        </div>
        <div class="footer-copyright">
          made by
          <a
            href="https://github.com/Belozerovmi"
            target="_blank"
            rel="noopener noreferrer"
            >belozerov</a
          >
        </div>
      </div>
    </footer>

    <script src="script.js" defer></script>
  </body>
</html>


========== D:\All Files\Pasta_Calculator-API--main\Pasta_Calculator-API--main\manifest.json ==========


{
  "name": "MacroCalc",
  "short_name": "MacroCalc",
  "description": "РљР°Р»СЊРєСѓР»СЏС‚РѕСЂ РїРѕСЂС†РёР№ РјР°РєР°СЂРѕРЅ",
  "start_url": "/",
  "scope": "/",
  "display": "standalone",
  "orientation": "portrait",
  "theme_color": "#1e2d4c",
  "background_color": "#ffffff",
  "icons": [
    {
      "src": "/icons/icon-72.png",
      "sizes": "72x72",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-96.png",
      "sizes": "96x96",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-128.png",
      "sizes": "128x128",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-144.png",
      "sizes": "144x144",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-152.png",
      "sizes": "152x152",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-256.png",
      "sizes": "256x256",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-384.png",
      "sizes": "384x384",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/icons/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}


========== D:\All Files\Pasta_Calculator-API--main\Pasta_Calculator-API--main\script.js ==========


// ---- РљР°С‚РµРіРѕСЂРёРё РїСЂРѕРґСѓРєС‚РѕРІ СЃ РїСЂРµРґСѓСЃС‚Р°РЅРѕРІРєР°РјРё ----
const productPresets = {
  pasta: {
    name: "РњР°РєР°СЂРѕРЅС‹ ",
    kcal: 354,
    protein: 12.0,
    fat: 1.5,
    carbs: 70.0,
  },
  rice: {
    name: "Р РёСЃ Р±РµР»С‹Р№",
    kcal: 330,
    protein: 7.5,
    fat: 0.8,
    carbs: 73.0,
  },
  grains: {
    name: "РљСЂСѓРїР°",
    kcal: 343,
    protein: 13.3,
    fat: 3.4,
    carbs: 71.5,
  },
};

let currentCategory = "pasta";
let currentProduct = { ...productPresets.pasta, barcode: null };

// ---- РСЃС‚РѕСЂРёСЏ РїСЂРѕРґСѓРєС‚РѕРІ ----
let productHistory = [];

// DOM СЌР»РµРјРµРЅС‚С‹
const productNameEl = document.getElementById("productName");
const kcalVal = document.getElementById("kcalVal");
const proteinVal = document.getElementById("proteinVal");
const fatVal = document.getElementById("fatVal");
const carbsVal = document.getElementById("carbsVal");
const dryTotal = document.getElementById("dryTotal");
const cookedTotal = document.getElementById("cookedTotal");
const portionCooked = document.getElementById("portionCooked");
const calcBtn = document.getElementById("calcBtn");
const scanBarcodeBtn = document.getElementById("scanBarcodeBtn");
const searchProductBtn = document.getElementById("searchProductBtn");
const modal = document.getElementById("modal");
const modalTitle = document.getElementById("modalTitle");
const searchInput = document.getElementById("searchInput");
const searchResultsDiv = document.getElementById("searchResults");
const scannerContainer = document.getElementById("scannerContainer");
const resultModal = document.getElementById("resultModal");
const modalDryPortion = document.getElementById("modalDryPortion");
const modalKbju = document.getElementById("modalKbju");
const modalCopyBtn = document.getElementById("modalCopyBtn");

let html5QrCode = null;
let isModalOpen = false;
let isProcessing = false;

// ---- РЎРњР•РќРђ РљРђРўР•Р“РћР РР (Р‘Р•Р— РЈР’Р•Р”РћРњР›Р•РќРР™) ----
function setupCategoryButtons() {
  const buttons = document.querySelectorAll(".category-btn");
  buttons.forEach((btn) => {
    btn.addEventListener("click", () => {
      const category = btn.dataset.category;
      if (!category || !productPresets[category]) return;

      buttons.forEach((b) => b.classList.remove("active"));
      btn.classList.add("active");

      currentCategory = category;
      currentProduct = { ...productPresets[category], barcode: null };
      updateProductUI();
      updateCookingHint(category);

      // Р—РЅР°С‡РµРЅРёСЏ РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ
      if (category === "pasta") {
        dryTotal.value = "100";
        cookedTotal.value = "240";
        portionCooked.value = "200";
      } else if (category === "rice") {
        dryTotal.value = "100";
        cookedTotal.value = "300";
        portionCooked.value = "200";
      } else if (category === "grains") {
        dryTotal.value = "100";
        cookedTotal.value = "250";
        portionCooked.value = "200";
      }

      // РЈРІРµРґРѕРјР»РµРЅРёРµ РЈР‘Р РђРќРћ вЂ” Р±РѕР»СЊС€Рµ РЅРµ Р±РµСЃРёС‚
    });
  });
}

// ---- РџРћРљРђР—РђРўР¬/РЎРљР Р«РўР¬ РРќР”РРљРђРўРћР  Р—РђР“Р РЈР—РљР ----
function showModalLoading(show, isSearch = true) {
  let existingLoader = document.querySelector(".modal-loader");
  if (show) {
    if (existingLoader) existingLoader.remove();
    const loader = document.createElement("div");
    loader.className = "modal-loader";
    loader.innerHTML = `
      <div class="loader-spinner"></div>
      <div class="loader-text">${isSearch ? "РџРѕРёСЃРє..." : "РЎРєР°РЅРёСЂРѕРІР°РЅРёРµ..."}</div>
    `;
    if (isSearch) {
      searchResultsDiv.innerHTML = "";
      searchResultsDiv.appendChild(loader);
    } else {
      scannerContainer.innerHTML = "";
      scannerContainer.appendChild(loader);
    }
  } else {
    if (existingLoader) existingLoader.remove();
  }
}

function setButtonLoading(button, isLoading) {
  if (isLoading) {
    button.dataset.originalText = button.innerHTML;
    button.innerHTML = `<div class="btn-loader"></div> Р—Р°РіСЂСѓР·РєР°...`;
    button.disabled = true;
  } else {
    button.innerHTML = button.dataset.originalText;
    button.disabled = false;
  }
}

// function showToast(message, isError = false) {
//   let toast = document.querySelector(".toast");
//   if (!toast) {
//     toast = document.createElement("div");
//     toast.className = "toast";
//     document.body.appendChild(toast);
//   }
//   toast.textContent = message;
//   toast.style.background = isError ? "#b4656d" : "#2e5a4b";
//   toast.classList.add("show");
//   setTimeout(() => {
//     toast.classList.remove("show");
//   }, 2500);
// }

function closeResultModal() {
  resultModal.style.display = "none";
}

let touchStartY = 0;
resultModal.addEventListener("touchstart", (e) => {
  if (
    e.target === resultModal ||
    e.target.closest(".result-modal-content") === null
  )
    return;
  touchStartY = e.touches[0].clientY;
});
resultModal.addEventListener("touchmove", (e) => {
  const deltaY = e.touches[0].clientY - touchStartY;
  if (deltaY > 50) closeResultModal();
});

function closeMainModal() {
  if (isProcessing) return;
  isModalOpen = false;
  modal.style.display = "none";
  if (html5QrCode) {
    html5QrCode.stop().catch(() => {});
    html5QrCode = null;
  }
  scannerContainer.style.display = "none";
  scannerContainer.innerHTML = "";
  searchResultsDiv.innerHTML = "";
  searchInput.value = "";
}

function showCustomProductDialog() {
  return new Promise((resolve) => {
    const overlay = document.createElement("div");
    overlay.className = "custom-dialog-overlay";
    overlay.innerHTML = `
      <div class="custom-dialog">
        <button class="custom-dialog-close">вњ•</button>
        <h4>РЎРѕР·РґР°С‚СЊ СЃРІРѕР№ РїСЂРѕРґСѓРєС‚</h4>
        <div class="dialog-field">
          <label>РќР°Р·РІР°РЅРёРµ РїСЂРѕРґСѓРєС‚Р°</label>
          <input type="text" id="dialog_name" placeholder="РЅР°РїСЂРёРјРµСЂ: РњР°РєР°СЂРѕРЅС‹ Barilla" value="РњРѕРё РјР°РєР°СЂРѕРЅС‹" maxlength="50">
          <div>
        <div class="dialog-field">
          <label>РљР°Р»РѕСЂРёР№РЅРѕСЃС‚СЊ (РєРєР°Р» РЅР° 100 Рі)</label>
          <input type="number" id="dialog_kcal" placeholder="350" value="350" step="1">
        </div>
        <div class="dialog-field">
          <label>Р‘РµР»РєРё (Рі РЅР° 100 Рі)</label>
          <input type="number" id="dialog_protein" placeholder="12" value="12" step="0.1">
        </div>
        <div class="dialog-field">
          <label>Р–РёСЂС‹ (Рі РЅР° 100 Рі)</label>
          <input type="number" id="dialog_fat" placeholder="1.5" value="1.5" step="0.1">
        </div>
        <div class="dialog-field">
          <label>РЈРіР»РµРІРѕРґС‹ (Рі РЅР° 100 Рі)</label>
          <input type="number" id="dialog_carbs" placeholder="70" value="70" step="0.1">
        </div>
        <div class="custom-dialog-buttons">
          <button class="dialog-confirm">РЎРѕС…СЂР°РЅРёС‚СЊ</button>
          <button class="dialog-cancel">РћС‚РјРµРЅР°</button>
        </div>
      </div>
    `;
    document.body.appendChild(overlay);

    const closeBtn = overlay.querySelector(".custom-dialog-close");
    const confirmBtn = overlay.querySelector(".dialog-confirm");
    const cancelBtn = overlay.querySelector(".dialog-cancel");

    const close = () => overlay.remove();
    const resolveAndClose = (result) => {
      close();
      resolve(result);
    };

    closeBtn.onclick = () => resolveAndClose(null);
    cancelBtn.onclick = () => resolveAndClose(null);
    overlay.onclick = (e) => {
      if (e.target === overlay) resolveAndClose(null);
    };

    confirmBtn.onclick = () => {
      const name = document.getElementById("dialog_name").value.trim();
      const kcal = parseFloat(document.getElementById("dialog_kcal").value);
      const protein = parseFloat(
        document.getElementById("dialog_protein").value,
      );
      const fat = parseFloat(document.getElementById("dialog_fat").value);
      const carbs = parseFloat(document.getElementById("dialog_carbs").value);
      if (!name) {
        showToast("Р’РІРµРґРёС‚Рµ РЅР°Р·РІР°РЅРёРµ РїСЂРѕРґСѓРєС‚Р°", true);
        return;
      }
      if (isNaN(kcal) || isNaN(protein) || isNaN(fat) || isNaN(carbs)) {
        showToast("Р—Р°РїРѕР»РЅРёС‚Рµ РІСЃРµ РїРѕР»СЏ РљР‘Р–РЈ РєРѕСЂСЂРµРєС‚РЅС‹РјРё С‡РёСЃР»Р°РјРё", true);
        return;
      }
      resolveAndClose({ name, kcal, protein, fat, carbs });
    };
  });
}

function loadHistory() {
  const saved = localStorage.getItem("macrocalc_history");
  if (saved) {
    try {
      productHistory = JSON.parse(saved);
      renderHistory();
    } catch (e) {}
  }
}

function saveHistory() {
  localStorage.setItem(
    "macrocalc_history",
    JSON.stringify(productHistory.slice(0, 5)),
  );
}

function addToHistory(product) {
  const index = productHistory.findIndex(
    (p) => p.name === product.name && p.kcal === product.kcal,
  );
  if (index !== -1) productHistory.splice(index, 1);
  productHistory.unshift({
    name: product.name,
    kcal: product.kcal,
    protein: product.protein,
    fat: product.fat,
    carbs: product.carbs,
    barcode: product.barcode,
  });
  if (productHistory.length > 5) productHistory.pop();
  saveHistory();
  renderHistory();
}

// ---- РџРћР›РЈР§Р•РќРР• РРљРћРќРљР Р”Р›РЇ РљРђРўР•Р“РћР РР ----
function getCategoryIcon(productName) {
  const name = productName.toLowerCase();
  if (
    name.includes("РјР°РєР°СЂРѕРЅ") ||
    name.includes("РїР°СЃС‚Р°") ||
    name.includes("СЃРїР°РіРµС‚С‚Рё")
  ) {
    return '<i class="fas fa-utensils" style="font-size: 18px; color: #1e2d4c;"></i>';
  }
  if (name.includes("СЂРёСЃ")) {
    return '<i class="fas fa-bowl-rice" style="font-size: 18px; color: #1e2d4c;"></i>';
  }
  if (
    name.includes("РіСЂРµС‡Рє") ||
    name.includes("РїРµСЂР»РѕРІ") ||
    name.includes("РѕРІСЃСЏРЅ") ||
    name.includes("Р±СѓР»РіСѓСЂ") ||
    name.includes("РєСЂСѓРїР°")
  ) {
    return '<i class="fas fa-seedling" style="font-size: 18px; color: #1e2d4c;"></i>';
  }
  // Р”РµС„РѕР»С‚РЅР°СЏ РёРєРѕРЅРєР°
  return '<i class="fas fa-utensil-spoon" style="font-size: 18px; color: #1e2d4c;"></i>';
}

function renderHistory() {
  let historySection = document.querySelector(".history-section");
  const container = document.querySelector(".container");

  if (!historySection) {
    historySection = document.createElement("div");
    historySection.className = "history-section";
    const actionRow = document.querySelector(".action-row");
    if (actionRow) {
      actionRow.insertAdjacentElement("afterend", historySection);
    } else {
      container.appendChild(historySection);
    }
  }

  if (productHistory.length === 0) {
    historySection.innerHTML = `
      <div class="history-title">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 8l0 4l2 2" /><path d="M3.05 11a9 9 0 1 1 .5 4m-.5 5v-5h5" /></svg>
        <span>РќРµРґР°РІРЅРёРµ РїСЂРѕРґСѓРєС‚С‹</span>
      </div>
      <div class="history-empty">вњЁ Р—РґРµСЃСЊ Р±СѓРґСѓС‚ РїРѕСЏРІР»СЏС‚СЊСЃСЏ РјР°РєР°СЂРѕРЅС‹, РєСЂСѓРїС‹ Рё СЂРёСЃ, РєРѕС‚РѕСЂС‹Рµ РІС‹ РёСЃРєР°Р»Рё</div>
    `;
    return;
  }

  historySection.innerHTML = `
    <div class="history-title">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 8l0 4l2 2" /><path d="M3.05 11a9 9 0 1 1 .5 4m-.5 5v-5h5" /></svg>
      <span>РќРµРґР°РІРЅРёРµ РїСЂРѕРґСѓРєС‚С‹</span>
      <button class="history-clear" id="clearHistoryBtn">РћС‡РёСЃС‚РёС‚СЊ</button>
    </div>
    <div class="history-list">
      ${productHistory
        .map(
          (p) => `
        <div class="history-item" data-name="${p.name.replace(/"/g, "&quot;")}" data-kcal="${p.kcal}" data-protein="${p.protein}" data-fat="${p.fat}" data-carbs="${p.carbs}" data-barcode="${p.barcode || ""}">
          <div class="history-item-icon">
            ${getCategoryIcon(p.name)}
          </div>
          <div class="history-item-info">
            <div class="history-item-name">${p.name.length > 18 ? p.name.slice(0, 16) + "..." : p.name}</div>
            <div class="history-item-kcal">${Math.round(p.kcal)} РєРєР°Р» / 100Рі</div>
          </div>
        </div>
      `,
        )
        .join("")}
    </div>
  `;

  document.querySelectorAll(".history-item").forEach((item) => {
    item.addEventListener("click", () => {
      currentProduct = {
        name: item.dataset.name,
        kcal: parseFloat(item.dataset.kcal),
        protein: parseFloat(item.dataset.protein),
        fat: parseFloat(item.dataset.fat),
        carbs: parseFloat(item.dataset.carbs),
        barcode: item.dataset.barcode || null,
      };
      updateProductUI();
      calculate();
      showToast(`Р’С‹Р±СЂР°РЅ: ${currentProduct.name}`);
    });
  });

  const clearBtn = document.getElementById("clearHistoryBtn");
  if (clearBtn) {
    clearBtn.onclick = () => {
      productHistory = [];
      saveHistory();
      renderHistory();
      showToast("РСЃС‚РѕСЂРёСЏ РѕС‡РёС‰РµРЅР°");
    };
  }
}

function updateProductUI() {
  productNameEl.innerText = truncateProductName(currentProduct.name, 32);
  productNameEl.title = currentProduct.name; // РџРѕР»РЅРѕРµ РЅР°Р·РІР°РЅРёРµ РІСЃРїР»С‹РІР°РµС‚ РїСЂРё РЅР°РІРµРґРµРЅРёРё
  kcalVal.innerText = Math.round(currentProduct.kcal);
  proteinVal.innerText = currentProduct.protein.toFixed(1);
  fatVal.innerText = currentProduct.fat.toFixed(1);
  carbsVal.innerText = currentProduct.carbs.toFixed(1);
}

function calculate() {
  let dry = parseFloat(dryTotal.value);
  let cooked = parseFloat(cookedTotal.value);
  let portion = parseFloat(portionCooked.value);
  if (
    isNaN(dry) ||
    isNaN(cooked) ||
    isNaN(portion) ||
    dry <= 0 ||
    cooked <= 0 ||
    portion <= 0
  ) {
    showToast("Р—Р°РїРѕР»РЅРёС‚Рµ РІСЃРµ РїРѕР»СЏ (>0)", true);
    return;
  }
  let dryPortion = (portion * dry) / cooked;
  dryPortion = Math.round(dryPortion * 10) / 10;

  let kcalPortion = (currentProduct.kcal * dryPortion) / 100;
  let proteinPortion = (currentProduct.protein * dryPortion) / 100;
  let fatPortion = (currentProduct.fat * dryPortion) / 100;
  let carbsPortion = (currentProduct.carbs * dryPortion) / 100;

  modalDryPortion.innerText = dryPortion;
  modalKbju.innerHTML = `${Math.round(kcalPortion)} РєРєР°Р» &nbsp;|&nbsp; ${proteinPortion.toFixed(1)}Рі Р±РµР» &nbsp;|&nbsp; ${fatPortion.toFixed(1)}Рі Р¶РёСЂ &nbsp;|&nbsp; ${carbsPortion.toFixed(1)}Рі СѓРіР»`;

  resultModal.style.display = "flex";
}

function copyResultFromModal() {
  let val = modalDryPortion.innerText;
  if (!val || val === "--") {
    showToast("РћС€РёР±РєР°", true);
    return;
  }
  navigator.clipboard.writeText(val);
  showToast(`РЎРєРѕРїРёСЂРѕРІР°РЅРѕ: ${val} Рі`);
  closeResultModal();
}

async function fetchProductByBarcode(barcode) {
  const url = `https://ru.openfoodfacts.org/api/v0/product/${barcode}.json`;
  try {
    const response = await fetch(url, {
      headers: { "User-Agent": "MacroCalc/2.0 (support@macrocalc.ru)" },
    });
    const data = await response.json();
    if (data.status === 1 && data.product) {
      const p = data.product;
      const nut = p.nutriments;
      let kcal = nut["energy-kcal_100g"] || nut["energy_100g"] || 350;
      let protein = nut["proteins_100g"] || 0;
      let fat = nut["fat_100g"] || 0;
      let carbs = nut["carbohydrates_100g"] || 0;
      currentProduct = {
        name: p.product_name || p.brands || "РџСЂРѕРґСѓРєС‚",
        kcal: kcal,
        protein: protein,
        fat: fat,
        carbs: carbs,
        barcode: barcode,
      };
      updateProductUI();
      calculate();
      addToHistory(currentProduct);
      showToast(`РќР°Р№РґРµРЅ: ${currentProduct.name}`);
      return true;
    } else {
      showToast(`РЁС‚СЂРёС…РєРѕРґ ${barcode} РЅРµ РЅР°Р№РґРµРЅ РІ Р±Р°Р·Рµ`, true);
      return false;
    }
  } catch (e) {
    console.error("РћС€РёР±РєР° API:", e);
    showToast("РћС€РёР±РєР° СЃРѕРµРґРёРЅРµРЅРёСЏ СЃ СЃРµСЂРІРµСЂРѕРј", true);
    return false;
  }
}

// ---- РџРћРРЎРљ РџР РћР”РЈРљРўРђ (РРЎРџР РђР’Р›Р•РќРќР«Р™) ----
let searchTimeout = null;
let isSearching = false;

async function searchProduct(query) {
  // РћС‡РёС‰Р°РµРј РїСЂРµРґС‹РґСѓС‰РёР№ С‚Р°Р№РјР°СѓС‚
  if (searchTimeout) clearTimeout(searchTimeout);

  // Р•СЃР»Рё Р·Р°РїСЂРѕСЃ СЃР»РёС€РєРѕРј РєРѕСЂРѕС‚РєРёР№ вЂ” РѕС‡РёС‰Р°РµРј СЂРµР·СѓР»СЊС‚Р°С‚С‹ Рё РІС‹С…РѕРґРёРј
  if (!query || query.trim().length < 2) {
    searchResultsDiv.innerHTML = "";
    return;
  }

  // Р”РµР»Р°РµРј debounce вЂ” Р¶РґС‘Рј 500ms РїРѕСЃР»Рµ РїРѕСЃР»РµРґРЅРµРіРѕ РІРІРѕРґР°
  searchTimeout = setTimeout(async () => {
    // РџСЂРµРґРѕС‚РІСЂР°С‰Р°РµРј РїРѕРІС‚РѕСЂРЅС‹Рµ РѕРґРЅРѕРІСЂРµРјРµРЅРЅС‹Рµ Р·Р°РїСЂРѕСЃС‹
    if (isSearching) return;
    isSearching = true;

    // РџРѕРєР°Р·С‹РІР°РµРј РёРЅРґРёРєР°С‚РѕСЂ Р·Р°РіСЂСѓР·РєРё
    showModalLoading(true, true);

    // РљРѕРґРёСЂСѓРµРј Р·Р°РїСЂРѕСЃ (API СЃР°Рј РѕР±СЂР°Р±РѕС‚Р°РµС‚ СЂРµРіРёСЃС‚СЂ)
    const url = `https://world.openfoodfacts.org/cgi/search.pl?search_terms=${encodeURIComponent(query)}&search_simple=1&action=process&json=1&page_size=20`;

    try {
      const resp = await fetch(url, {
        headers: { "User-Agent": "MacroCalc/2.0" },
      });
      const data = await resp.json();

      // РЈР±РёСЂР°РµРј РёРЅРґРёРєР°С‚РѕСЂ Р·Р°РіСЂСѓР·РєРё
      showModalLoading(false, true);
      searchResultsDiv.innerHTML = "";

      if (data.products && data.products.length > 0) {
        // Р¤РёР»СЊС‚СЂСѓРµРј С‚РѕР»СЊРєРѕ РїСЂРѕРґСѓРєС‚С‹ СЃ РљР‘Р–РЈ Рё РЅРѕСЂРјР°Р»РёР·СѓРµРј СЂРµРіРёСЃС‚СЂ РґР»СЏ РѕС‚РѕР±СЂР°Р¶РµРЅРёСЏ
        const validProducts = data.products.filter((prod) => prod.nutriments);

        if (validProducts.length === 0) {
          searchResultsDiv.innerHTML =
            "<div style='padding:16px;color:#858585;text-align:center'>РќРµС‚ РїСЂРѕРґСѓРєС‚РѕРІ СЃ РїРѕР»РЅС‹РјРё РґР°РЅРЅС‹РјРё РљР‘Р–РЈ</div>";
        } else {
          validProducts.forEach((prod) => {
            const nut = prod.nutriments;
            const div = document.createElement("div");
            div.className = "search-item";

            // РџСЂРёРІРѕРґРёРј РЅР°Р·РІР°РЅРёРµ Рє РЅРѕСЂРјР°Р»СЊРЅРѕРјСѓ РІРёРґСѓ (РїРµСЂРІР°СЏ Р±СѓРєРІР° Р·Р°РіР»Р°РІРЅР°СЏ)
            let productName = prod.product_name || "Р‘РµР· РЅР°Р·РІР°РЅРёСЏ";
            // РџСЂРѕСЃС‚РѕРµ РїСЂРёРІРµРґРµРЅРёРµ РїРµСЂРІРѕР№ Р±СѓРєРІС‹ Рє Р·Р°РіР»Р°РІРЅРѕР№ РґР»СЏ РєСЂР°СЃРѕС‚С‹
            if (productName !== "Р‘РµР· РЅР°Р·РІР°РЅРёСЏ" && productName.length > 0) {
              productName =
                productName.charAt(0).toUpperCase() + productName.slice(1);
            }

            div.innerHTML = `<strong>${productName}</strong><br>
              <span style="font-size:13px;color:#858585">
                ${nut["energy-kcal_100g"] || "?"} РєРєР°Р» В· 
                Р±РµР»РєРё ${nut["proteins_100g"] || "?"}Рі В· 
                Р¶РёСЂС‹ ${nut["fat_100g"] || "?"}Рі В· 
                СѓРіР»РµРІРѕРґС‹ ${nut["carbohydrates_100g"] || "?"}Рі
              </span>`;

            div.onclick = () => {
              currentProduct = {
                name: productName,
                kcal: nut["energy-kcal_100g"] || 350,
                protein: nut["proteins_100g"] || 10,
                fat: nut["fat_100g"] || 1,
                carbs: nut["carbohydrates_100g"] || 70,
                barcode: prod.code,
              };
              updateProductUI();
              calculate();
              addToHistory(currentProduct);
              closeMainModal();
              showToast(`Р’С‹Р±СЂР°РЅ: ${currentProduct.name}`);
            };
            searchResultsDiv.appendChild(div);
          });
        }
      } else {
        searchResultsDiv.innerHTML =
          "<div style='padding:16px;color:#858585;text-align:center'>РќРёС‡РµРіРѕ РЅРµ РЅР°Р№РґРµРЅРѕ. РџРѕРїСЂРѕР±СѓР№С‚Рµ РґСЂСѓРіРѕРµ РЅР°Р·РІР°РЅРёРµ РёР»Рё СЃРѕР·РґР°Р№С‚Рµ СЃРІРѕР№ РїСЂРѕРґСѓРєС‚.</div>";
      }
    } catch (err) {
      console.error("РћС€РёР±РєР° РїРѕРёСЃРєР°:", err);
      showModalLoading(false, true);
      searchResultsDiv.innerHTML =
        "<div style='padding:16px;color:#b4656d;text-align:center'>РћС€РёР±РєР° СЃРѕРµРґРёРЅРµРЅРёСЏ. РџСЂРѕРІРµСЂСЊС‚Рµ РёРЅС‚РµСЂРЅРµС‚ Рё РїРѕРїСЂРѕР±СѓР№С‚Рµ СЃРЅРѕРІР°.</div>";
    } finally {
      isSearching = false;
    }
  }, 500); // Р—Р°РґРµСЂР¶РєР° 500ms вЂ” РїРѕР»СЊР·РѕРІР°С‚РµР»СЊ СѓСЃРїРµРІР°РµС‚ РІРІРµСЃС‚Рё СЃР»РѕРІРѕ
}

async function handleCreateProduct() {
  const result = await showCustomProductDialog();
  if (result) {
    currentProduct = {
      name: result.name,
      kcal: result.kcal,
      protein: result.protein,
      fat: result.fat,
      carbs: result.carbs,
      barcode: null,
    };
    updateProductUI();
    calculate();
    addToHistory(currentProduct);
    showToast(`РЎРѕР·РґР°РЅ: ${currentProduct.name}`);
  }
}

async function startScanner() {
  if (!isModalOpen || isProcessing) return;

  isProcessing = true;
  modalTitle.innerText = "РЎРєР°РЅРёСЂРѕРІР°РЅРёРµ С€С‚СЂРёС…РєРѕРґР°";
  scannerContainer.style.display = "block";
  searchInput.style.display = "none";
  searchResultsDiv.style.display = "none";
  scannerContainer.innerHTML = "";
  showModalLoading(true, false);

  if (html5QrCode) {
    try {
      await html5QrCode.stop();
      await html5QrCode.clear();
    } catch (e) {}
    html5QrCode = null;
  }

  html5QrCode = new Html5Qrcode("scannerContainer");

  const config = {
    fps: 15,
    qrbox: { width: 280, height: 200 },
    aspectRatio: 1.333,
  };

  try {
    await html5QrCode.start(
      { facingMode: "environment" },
      config,
      async (decodedText) => {
        if (html5QrCode) {
          try {
            await html5QrCode.stop();
            await html5QrCode.clear();
          } catch (e) {}
          html5QrCode = null;
        }

        showModalLoading(true, false);
        const success = await fetchProductByBarcode(decodedText);
        showModalLoading(false, false);
        closeMainModal();
        isProcessing = false;
      },
      (error) => {
        if (error && error.includes("NotFoundException")) return;
        console.warn("РћС€РёР±РєР° СЃРєР°РЅРёСЂРѕРІР°РЅРёСЏ:", error);
      },
    );
    showModalLoading(false, false);
  } catch (err) {
    console.error("РћС€РёР±РєР° Р·Р°РїСѓСЃРєР° РєР°РјРµСЂС‹:", err);
    showModalLoading(false, false);
    showToast("РќРµ СѓРґР°Р»РѕСЃСЊ Р·Р°РїСѓСЃС‚РёС‚СЊ РєР°РјРµСЂСѓ. РџСЂРѕРІРµСЂСЊС‚Рµ СЂР°Р·СЂРµС€РµРЅРёСЏ.", true);
    closeMainModal();
    isProcessing = false;
  }
}

function openSearchModal() {
  if (isProcessing) return;
  isModalOpen = true;
  isProcessing = false;
  modalTitle.innerText = "РџРѕРёСЃРє РїСЂРѕРґСѓРєС‚Р°";
  scannerContainer.style.display = "none";
  searchInput.style.display = "block";
  searchResultsDiv.style.display = "block";
  searchInput.value = "";
  searchResultsDiv.innerHTML = "";
  if (html5QrCode) {
    html5QrCode.stop().catch(() => {});
    html5QrCode = null;
  }
  searchInput.oninput = (e) => searchProduct(e.target.value);
  modal.style.display = "flex";
}

function openScannerModal() {
  if (isProcessing) return;
  isModalOpen = true;
  isProcessing = false;
  modalTitle.innerText = "РЎРєР°РЅРµСЂ С€С‚СЂРёС…РєРѕРґР°";
  scannerContainer.style.display = "block";
  searchInput.style.display = "none";
  searchResultsDiv.style.display = "none";
  if (html5QrCode) {
    html5QrCode.stop().catch(() => {});
    html5QrCode = null;
  }
  modal.style.display = "flex";
  setTimeout(() => {
    if (modal.style.display === "flex") {
      startScanner();
    }
  }, 300);
}

// ---- РРќРР¦РРђР›РР—РђР¦РРЇ ----
calcBtn.onclick = calculate;
scanBarcodeBtn.onclick = openScannerModal;
searchProductBtn.onclick = openSearchModal;
document.getElementById("editProductBtn").onclick = handleCreateProduct;

modalCopyBtn.onclick = copyResultFromModal;

document.getElementById("resultModalCloseX").onclick = closeResultModal;
document.getElementById("mainModalCloseX").onclick = closeMainModal;

resultModal.onclick = (e) => {
  if (e.target === resultModal) closeResultModal();
};
modal.onclick = (e) => {
  if (e.target === modal && !isProcessing) closeMainModal();
};

// ---- РџРћР”РЎРљРђР—РљРђ РџРћ Р РђР—Р’РђР РР’РђРќРР® ----
const cookingRatios = {
  pasta: { dry: 100, cooked: 240, text: "100 Рі СЃСѓС…РёС… РјР°РєР°СЂРѕРЅ в‰€ 240 Рі РІР°СЂС‘РЅС‹С…" },
  rice: { dry: 100, cooked: 300, text: "100 Рі СЃСѓС…РѕРіРѕ СЂРёСЃР° в‰€ 300 Рі РІР°СЂС‘РЅРѕРіРѕ" },
  grains: { dry: 100, cooked: 250, text: "100 Рі СЃСѓС…РѕР№ РєСЂСѓРїС‹ в‰€ 250 Рі РІР°СЂС‘РЅРѕР№" },
};

function updateCookingHint(category) {
  const hintSpan = document.getElementById("hintText");
  if (hintSpan && cookingRatios[category]) {
    hintSpan.textContent = cookingRatios[category].text;
  }
}
// ---- РџР›РђР’РќРђРЇ РџР•Р Р•РњР•Р©РђР®Р©РђРЇРЎРЇ РџР›РђРЁРљРђ ----
function updateSliderPosition() {
  const activeBtn = document.querySelector(".category-btn.active");
  const slider = document.getElementById("sliderIndicator");
  const selector = document.getElementById("categorySelector");

  if (!activeBtn || !slider || !selector) return;

  const btnRect = activeBtn.getBoundingClientRect();
  const selectorRect = selector.getBoundingClientRect();

  // Р’С‹С‡РёСЃР»СЏРµРј РѕС‚СЃС‚СѓРї СЃР»РµРІР° Рё С€РёСЂРёРЅСѓ
  const left = btnRect.left - selectorRect.left;
  const width = btnRect.width;

  slider.style.left = `${left}px`;
  slider.style.width = `${width}px`;
}

// РћР±РЅРѕРІР»С‘РЅРЅР°СЏ С„СѓРЅРєС†РёСЏ СЃРјРµРЅС‹ РєР°С‚РµРіРѕСЂРёРё
function setupCategoryButtons() {
  const buttons = document.querySelectorAll(".category-btn");

  // РЈСЃС‚Р°РЅР°РІР»РёРІР°РµРј РЅР°С‡Р°Р»СЊРЅСѓСЋ РїРѕР·РёС†РёСЋ РїР»Р°С€РєРё
  setTimeout(() => updateSliderPosition(), 10);

  // РџСЂРё СЂРµСЃР°Р№Р·Рµ РѕРєРЅР° вЂ” РїРµСЂРµСЃС‡РёС‚С‹РІР°РµРј РїРѕР·РёС†РёСЋ
  window.addEventListener("resize", () => updateSliderPosition());

  buttons.forEach((btn) => {
    btn.addEventListener("click", () => {
      const category = btn.dataset.category;
      if (!category || !productPresets[category]) return;

      // РњРµРЅСЏРµРј Р°РєС‚РёРІРЅС‹Р№ РєР»Р°СЃСЃ
      buttons.forEach((b) => b.classList.remove("active"));
      btn.classList.add("active");

      // РџРµСЂРµРјРµС‰Р°РµРј РїР»Р°С€РєСѓ (CSS СЃР°Рј СЃРґРµР»Р°РµС‚ Р°РЅРёРјР°С†РёСЋ)
      updateSliderPosition();

      // РћР±РЅРѕРІР»СЏРµРј РґР°РЅРЅС‹Рµ РїСЂРѕРґСѓРєС‚Р°
      currentCategory = category;
      currentProduct = { ...productPresets[category], barcode: null };
      updateProductUI();
      updateCookingHint(category);

      // РџРѕРґСЃС‚Р°РІР»СЏРµРј Р·РЅР°С‡РµРЅРёСЏ РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ
      const defaults = {
        pasta: { dry: 100, cooked: 240, portion: 200 },
        rice: { dry: 100, cooked: 300, portion: 200 },
        grains: { dry: 100, cooked: 250, portion: 200 },
      };

      const def = defaults[category] || defaults.pasta;
      dryTotal.value = def.dry;
      cookedTotal.value = def.cooked;
      portionCooked.value = def.portion;
    });
  });
}

// ---- РћР‘Р Р•Р—РљРђ Р”Р›РРќРќРћР“Рћ РќРђР—Р’РђРќРРЇ РџР РћР”РЈРљРўРђ ----
function truncateProductName(name, maxLength = 35) {
  if (!name) return "РџСЂРѕРґСѓРєС‚";
  if (name.length <= maxLength) return name;
  return name.slice(0, maxLength) + "...";
}

setupCategoryButtons();
loadHistory();
updateProductUI();


========== D:\All Files\Pasta_Calculator-API--main\Pasta_Calculator-API--main\style.css ==========


* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  -webkit-tap-highlight-color: transparent;
}

body {
  font-family:
    system-ui,
    -apple-system,
    "Segoe UI",
    Roboto,
    Helvetica,
    Arial,
    sans-serif;
  background: #ffffff;
  color: #1e2d4c;
  padding: 24px 20px 48px;
  transition: background 0.2s;
}

.container {
  max-width: 560px;
  margin: 0 auto;
}

/* ===== TYPOGRAPHY ===== */
h1 {
  font-size: 26px;
  font-weight: 600;
  letter-spacing: -0.3px;
  color: #1e2d4c;
  margin-bottom: 6px;
}
.subhead {
  font-size: 14px;
  color: #858585;
  border-left: 2px solid #acbdaa;
  padding-left: 12px;
  margin-bottom: 28px;
}

/* ===== PRODUCT CARD ===== */
.product-card {
  background: #f6f8f4;
  border-radius: 10px;
  padding: 20px;
  margin-bottom: 24px;
  /* border: 1px solid #e2e7dd; */
}
.product-name-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16px;
  flex-wrap: wrap;
  gap: 8px;
}
.product-name {
  font-size: 20px;
  font-weight: 600;
  color: #1e2d4c;
  word-break: break-word;
}
.edit-link {
  color: #858585;
  font-size: 13px;
  font-weight: 500;
  background: transparent;
  border: none;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  cursor: pointer;
  padding: 6px 6px;
  border-radius: 40px;
  transition: background 0.2s;
}
.edit-link:hover {
  background: #e2e7dd;
}
.nutrients {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  background: #ffffff;
  padding: 14px 12px;
  border-radius: 24px;
  margin-top: 8px;
  border: 1px solid #e2e7dd;
}
.nutrient {
  text-align: center;
  flex: 1;
}
.nutrient-value {
  font-weight: 700;
  font-size: 18px;
  color: #1e2d4c;
}
.nutrient-label {
  font-size: 11px;
  color: #858585;
  margin-top: 4px;
}

/* ===== HISTORY SECTION ===== */
.history-section {
  margin: 24px 0 20px;
}

.history-title {
  font-size: 13px;
  font-weight: 600;
  color: #858585;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin-bottom: 12px;
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 0 4px;
}

.history-clear {
  background: none;
  border: none;
  font-size: 12px;
  color: #acbdaa;
  cursor: pointer;
  padding: 4px 8px;
  border-radius: 20px;
  transition: all 0.2s;
}

.history-clear:active {
  background: #f0f3ec;
  color: #1e2d4c;
}

.history-list {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
  overflow-x: auto;
  padding-bottom: 12px;
  scrollbar-width: thin;
}

.history-list::-webkit-scrollbar {
  height: 4px;
}

.history-list::-webkit-scrollbar-track {
  background: #f0f3ec;
  border-radius: 10px;
}

.history-list::-webkit-scrollbar-thumb {
  background: #acbdaa;
  border-radius: 10px;
}

/* РЈРІРµР»РёС‡РµРЅРЅС‹Рµ РїР»Р°С€РєРё РёСЃС‚РѕСЂРёРё */
.history-item {
  background: #f6f8f4;
  border: 1px solid #e2e7dd;
  border-radius: 24px;
  padding: 14px 18px;
  min-width: 160px;
  cursor: pointer;
  transition: all 0.2s ease;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  gap: 12px;
}

.history-item:active {
  background: #e2e7dd;
  transform: scale(0.97);
}

/* РРєРѕРЅРєР° РєР°С‚РµРіРѕСЂРёРё РІ РёСЃС‚РѕСЂРёРё */
.history-item-icon {
  width: 40px;
  height: 40px;
  background: white;
  border-radius: 30px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
}

.history-item-info {
  flex: 1;
}

.history-item-name {
  font-weight: 600;
  font-size: 14px;
  color: #1e2d4c;
  margin-bottom: 4px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 110px;
}

.history-item-kcal {
  font-size: 11px;
  color: #858585;
}

.history-empty {
  color: #858585;
  font-size: 13px;
  padding: 20px 12px;
  text-align: center;
  background: #f6f8f4;
  border-radius: 24px;
}

/* ===== INPUT FIELDS ===== */
.input-group {
  margin-bottom: 20px;
}
.input-group label {
  display: block;
  font-size: 14px;
  font-weight: 500;
  color: #1e2d4c;
  margin-bottom: 8px;
}
.input-wrapper {
  background: #f6f8f4;
  border-radius: 10px;
  padding: 4px 16px;
  display: flex;
  align-items: center;
  /* border: 1px solid #e2e7dd; */
  transition: all 0.2s;
}
.input-wrapper:focus-within {
  border-color: #acbdaa;
  box-shadow: 0 0 0 3px rgba(172, 189, 170, 0.2);
}
.input-wrapper input {
  width: 100%;
  padding: 16px 0;
  font-size: 18px;
  border: none;
  background: transparent;
  color: #1e2d4c;
  font-weight: 500;
  outline: none;
}
.input-unit {
  color: #858585;
  font-size: 16px;
  padding-left: 8px;
}

/* ===== BUTTONS ===== */
.btn {
  border: none;
  font-weight: 600;
  border-radius: 44px;
  padding: 14px 20px;
  font-size: 16px;
  cursor: pointer;
  transition: 0.2s;
  width: 100%;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
}
.btn-primary {
  background: #1e2d4c;
  color: white;
  margin: 16px 0 0px;
}
.btn-primary:active {
  background: #0f1a2e;
  transform: scale(0.97);
}
.btn-outline {
  background: transparent;
  border: 1.5px solid #acbdaa;
  color: #1e2d4c;
}
.action-row {
  display: flex;
  gap: 12px;
  margin: 16px 0 20px;
}
.action-row .btn {
  margin: 0;
}

/* ===== RESULT CARD (СЃРєСЂС‹С‚) ===== */
.result-card {
  display: none;
}

/* ===== RESULT MODAL ===== */
.result-modal {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.7);
  align-items: center;
  justify-content: center;
  z-index: 1300;
  backdrop-filter: blur(4px);
}
.result-modal-content {
  position: relative;
  background: white;
  max-width: 320px;
  width: 85%;
  border-radius: 25px;
  padding: 32px 24px;
  text-align: center;
  box-shadow: 0 25px 45px -12px rgba(0, 0, 0, 0.3);
  animation: modalFadeIn 0.3s ease-out;
}
.result-modal-close {
  position: absolute;
  top: 16px;
  right: 20px;
  background: none;
  border: none;
  font-size: 24px;
  color: #858585;
  cursor: pointer;
  padding: 8px;
  line-height: 1;
  border-radius: 30px;
  transition: background 0.2s;
}
.result-modal-close:active {
  background: #f0f0f0;
}
@keyframes modalFadeIn {
  from {
    opacity: 0;
    transform: scale(0.95) translateY(10px);
  }
  to {
    opacity: 1;
    transform: scale(1) translateY(0);
  }
}
.result-modal-icon {
  width: 56px;
  height: 56px;
  background: #acbdaa;
  border-radius: 60px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 16px;
}
.result-modal-icon svg {
  width: 32px;
  height: 32px;
  stroke: #1e2d4c;
  stroke-width: 1.5;
}
.result-modal-label {
  font-size: 13px;
  font-weight: 500;
  letter-spacing: 0.5px;
  color: #858585;
  text-transform: uppercase;
  margin-bottom: 8px;
}
.result-modal-value {
  font-size: 56px;
  font-weight: 800;
  color: #1e2d4c;
  line-height: 1.1;
  margin-bottom: 8px;
}
.result-modal-unit {
  font-size: 20px;
  font-weight: 500;
  color: #858585;
}
.result-modal-kbju {
  font-size: 13px;
  color: #858585;
  margin: 16px 0 24px;
  padding-top: 12px;
  border-top: 1px solid #e2e7dd;
}
.result-modal-actions {
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.result-modal-btn {
  border: none;
  padding: 14px 20px;
  border-radius: 44px;
  font-size: 16px;
  font-weight: 400;
  cursor: pointer;
  transition: 0.2s;
  width: 100%;
}
.result-modal-btn-primary {
  background: #1e2d4c;
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
}
.result-modal-btn-primary:active {
  background: #0f1a2e;
  transform: scale(0.97);
}

/* ===== MAIN MODAL (РїРѕРёСЃРє/СЃРєР°РЅРµСЂ) ===== */
.modal {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.6);
  align-items: center;
  justify-content: center;
  z-index: 1000;
}
.modal-content {
  position: relative;
  background: white;
  max-width: 92%;
  width: 460px;
  border-radius: 36px;
  padding: 24px 20px;
  max-height: 85vh;
  overflow-y: auto;
  box-shadow: 0 20px 35px -10px rgba(0, 0, 0, 0.2);
}
.modal-close {
  position: absolute;
  top: 16px;
  right: 18px;
  background: none;
  border: none;
  font-size: 22px;
  color: #8585855f;
  cursor: pointer;
  padding: 8px;
  line-height: 1;
  border-radius: 30px;
  transition: background 0.2s;
}
.modal-close:active {
  background: #f0f0f0;
}
.modal-content h3 {
  font-size: 22px;
  font-weight: 600;
  color: #1e2d4c;
  margin-bottom: 18px;
  padding-right: 30px;
}
.search-input {
  width: 100%;
  padding: 14px 18px;
  border-radius: 40px;
  border: 1px solid #e2e7dd;
  background: #f6f8f4;
  font-size: 16px;
  /* margin-bottom: 20px; */
}

input:focus {
  outline: 1px solid #acacac;
}
.search-results {
  max-height: 320px;
  overflow-y: auto;
  margin-bottom: 16px;
}
.search-item {
  padding: 14px;
  border-bottom: 1px solid #ecf0e8;
  cursor: pointer;
  transition: background 0.1s;
}
.search-item:active {
  background: #f0f3ec;
}
#scannerContainer {
  width: 100%;
  margin: 10px 0;
  border-radius: 24px;
  overflow: hidden;
  min-height: 400px;
  background: #000;
}
#scannerContainer video {
  width: 100%;
  height: auto;
  object-fit: cover;
}

/* ===== TOAST NOTIFICATION ===== */
.toast {
  display: flex;
  position: fixed;
  top: 20px;
  left: 50%;
  transform: translateX(-50%) translateY(-100px);
  background: #2e5a4b;
  color: #fff;
  padding: 12px 24px;
  border-radius: 60px;
  font-size: 15px;
  font-weight: 500;
  z-index: 1100;
  opacity: 0;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
  pointer-events: none;
  width: 90%;
  max-width: 400px;
  white-space: normal;
  text-align: center;
  align-items: center;
  justify-content: center;
}
.toast.show {
  transform: translateX(-50%) translateY(0);
  opacity: 1;
}

/* ===== CUSTOM DIALOG (СЃРѕР·РґР°РЅРёРµ РїСЂРѕРґСѓРєС‚Р°) ===== */
.custom-dialog-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1200;
}
.custom-dialog {
  position: relative;
  background: white;
  border-radius: 15px;
  padding: 28px 24px 24px;
  width: 320px;
  max-width: 95%;
}
.custom-dialog-close {
  position: absolute;
  top: 14px;
  right: 16px;
  background: none;
  border: none;
  font-size: 22px;
  color: #8585855f;
  cursor: pointer;
  padding: 6px;
  line-height: 1;
  border-radius: 30px;
}
.custom-dialog-close:active {
  background: #f0f0f0;
}
.custom-dialog h4 {
  font-size: 18px;
  margin-bottom: 20px;
  color: #1e2d4c;
  padding-right: 24px;
}
.dialog-field {
  margin-bottom: 14px;
}
.dialog-field label {
  display: block;
  font-size: 13px;
  font-weight: 500;
  color: #1e2d4c;
  margin-bottom: 6px;
}
.dialog-field input {
  width: 100%;
  padding: 14px;
  border: 1px solid #e2e7dd;
  border-radius: 10px;
  background: #f6f8f4;
  font-size: 16px;
}
.dialog-field input:focus {
  outline: none;
  border-color: #acbdaa;
}
.custom-dialog-buttons {
  margin-top: 20px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.custom-dialog-buttons button {
  padding: 12px;
  border-radius: 30px;
  font-weight: 600;
  cursor: pointer;
}
.custom-dialog .dialog-confirm {
  background: #1e2d4c;
  color: white;
  border: none;
}
.custom-dialog .dialog-cancel {
  background: transparent;
  border: 1px solid #acbdaa;
  color: #1e2d4c;
}

.icon-svg {
  width: 20px;
  height: 20px;
  display: inline-block;
  vertical-align: middle;
}
.flex-between {
  display: flex;
  gap: 10px;
  align-items: center;
}
.flex-between a {
  display: flex;
  align-items: center;
  gap: 10px;
  text-decoration: none;
}
.flex-between img {
  width: 42px;
  height: 42px;
}
/* ===== LOADERS FOR MODALS ===== */
.modal-loader {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 40px 20px;
  gap: 16px;
}
.loader-spinner {
  width: 40px;
  height: 40px;
  border: 3px solid #e2e7dd;
  border-top-color: #1e2d4c;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}
.loader-text {
  font-size: 14px;
  color: #858585;
}
@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

.btn-loader {
  width: 20px;
  height: 20px;
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-top-color: white;
  border-radius: 50%;
  animation: spin 0.6s linear infinite;
  display: inline-block;
}

/* ===== FOOTER ===== */
.footer {
  margin-top: 48px;
  padding: 24px 0 16px;
  border-top: 1px solid #e2e7dd;
  background: #ffffff;
}

.footer-content {
  max-width: 560px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
}

.footer-logo {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 15px;
  font-weight: 500;
  color: #858585;
}

.footer-logo img {
  width: 24px;
  height: 24px;
}

.footer-links {
  display: flex;
  gap: 24px;
}

.footer-link {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  color: #858585;
  text-decoration: none;
  font-size: 14px;
  transition:
    color 0.2s,
    transform 0.1s;
}

.footer-link:active {
  transform: scale(0.96);
}

.footer-link:hover {
  color: #1e2d4c;
}

.footer-copyright {
  font-size: 13px;
  color: #acbdaa;
}

.footer-copyright a {
  color: #acbdaa;
  text-decoration: none;
  font-weight: 500;
  transition: color 0.2s;
}

.footer-copyright a:active {
  color: #1e2d4c;
}

.footer-copyright a:hover {
  color: #1e2d4c;
}
/* ===== CATEGORY SELECTOR ===== */
.category-selector {
  display: flex;
  gap: 10px;
  margin-bottom: 12px;
  background: #f6f8f4;
  padding: 6px;
  border-radius: 60px;
}

.category-btn {
  flex: 1;
  padding: 10px 12px;
  border: none;
  background: transparent;
  border-radius: 50px;
  font-size: 14px;
  font-weight: 500;
  color: #858585;
  cursor: pointer;
  transition: all 200ms ease-in-out;
  font-family: inherit;
}

.category-btn.active {
  background: #1e2d4c;
  color: white;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

.category-btn:active {
  transform: scale(0.96);
}
/* ===== TOOLTIP / HINT ===== */
.cooking-hint {
  background: #f0f3ec;
  border-radius: 10px;
  padding: 12px 16px;
  margin: 12px 0 12px;
  font-size: 13px;
  color: #1e2d4c;
  display: flex;
  align-items: center;
  gap: 10px;
}

.cooking-hint svg {
  flex-shrink: 0;
  color: #acbdaa;
}

.cooking-hint-text {
  line-height: 1.4;
}

.cooking-hint strong {
  color: #1e2d4c;
}

/* ===== CATEGORY SELECTOR ===== */
.category-selector {
  position: relative;
  display: flex;
  gap: 8px;
  margin-bottom: 12px;
  background: #f0f3ec;
  padding: 6px;
  border-radius: 60px;
}

/* РЎРєРѕР»СЊР·СЏС‰Р°СЏ РїР»Р°С€РєР° */
.slider-indicator {
  position: absolute;
  top: 6px;
  bottom: 6px;
  background: #1e2d4c;
  border-radius: 50px;
  transition: left 0.35s cubic-bezier(0.2, 0.9, 0.4, 1.1);
  z-index: 1;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
}

.category-btn {
  position: relative;
  z-index: 2;
  flex: 1;
  padding: 10px 12px;
  border: none;
  background: transparent;
  border-radius: 50px;
  font-size: 15px;
  font-weight: 500;
  color: #858585;
  cursor: pointer;
  transition: all 200ms ease-in-out;
  font-family: inherit;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
}

.category-btn i {
  font-size: 16px;
  transition: transform 0.2s ease;
}

.category-btn:active i {
  transform: scale(0.92);
}

.category-btn.active {
  color: white;
}

.category-btn:not(.active):hover {
  color: #1e2d4c;
}

.search-results {
  max-height: 320px;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  scroll-behavior: smooth;
}

/* РЎС‚РёР»Рё СЃРєСЂРѕР»Р»Р±Р°СЂР° РєР°Рє РІ macOS */
.search-results::-webkit-scrollbar {
  width: 5px;
}

.search-results::-webkit-scrollbar-track {
  background: transparent;
}

.search-results::-webkit-scrollbar-thumb {
  background: rgba(172, 189, 170, 0.6);
  border-radius: 20px;
  transition: background 0.2s;
}

.search-results::-webkit-scrollbar-thumb:hover {
  background: rgba(30, 45, 76, 0.7);
}

.search-results:hover::-webkit-scrollbar-thumb {
  background: rgba(172, 189, 170, 0.8);
}

.edit-link-text {
  display: none;
}

/* ===== РћР‘Р Р•Р—РљРђ Р”Р›РРќРќРћР“Рћ РќРђР—Р’РђРќРРЇ РџР РћР”РЈРљРўРђ ===== */
.product-name-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16px;
  flex-wrap: nowrap;
  gap: 12px;
}

.product-name {
  font-size: 20px;
  font-weight: 600;
  color: #1e2d4c;
  word-break: break-word;
  flex: 1;
  min-width: 0; /* Р’Р°Р¶РЅРѕ РґР»СЏ СЂР°Р±РѕС‚С‹ text-overflow */
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* Р”Р»СЏ РјРѕР±РёР»СЊРЅС‹С… СѓСЃС‚СЂРѕР№СЃС‚РІ вЂ” РїРµСЂРµРЅРѕСЃ РЅР° РґРІРµ СЃС‚СЂРѕРєРё, РµСЃР»Рё РѕС‡РµРЅСЊ РґР»РёРЅРЅРѕРµ */
@media (max-width: 480px) {
  /* .product-name-row {
    flex-wrap: wrap;
  } */

  .product-name {
    /* white-space: normal; */
    line-height: 1.3;
    flex: 1 1 100%;
  }

  .edit-link {
    align-self: flex-start;
  }
}

.edit-link {
  color: #858585;
  font-size: 13px;
  font-weight: 500;
  background: transparent;
  border: none;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  cursor: pointer;
  padding: 8px 12px;
  border-radius: 40px;
  transition: background 0.2s;
  flex-shrink: 0; /* РљРЅРѕРїРєР° РЅРµ СЃР¶РёРјР°РµС‚СЃСЏ */
}

.edit-link:hover {
  background: #e2e7dd;
}

.edit-link-text {
  display: none;
}

@media (max-width: 375px) {
  /* .edit-link-text {
    display: inline;
  } */

  .edit-link svg {
    width: 18px;
    height: 18px;
  }
}

@media (max-width: 400px) {
  .nutrients {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 375px) {
  .category-selector {
    flex-direction: column;
    border-radius: 10px;
    transition: all 200ms ease-in-out;
  }

  .category-btn.active {
    transform: none;
    border-radius: 10px;
  }

  .category-btn {
    border-radius: 10px;
  }

  .slider-indicator {
    display: none;
  }

  /* .edit-link-text {
    display: flex;
  } */

  /* .edit-link {
    position: absolute;
    right: 0px;
    top: -5px;
  }
*/
  /* .product-card {
    position: relative;
  } */

  .product-name-row {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 16px;
    /* flex-wrap: wrap; */
    gap: 8px;
    /* flex-direction: column; */
  }
}


========== D:\All Files\Pasta_Calculator-API--main\Pasta_Calculator-API--main\sw.js ==========


const CACHE_NAME = "macrocalc-v1";

// Р¤Р°Р№Р»С‹ РґР»СЏ РєСЌС€РёСЂРѕРІР°РЅРёСЏ
const urlsToCache = [
  "/",
  "/index.html",
  "/style.css",
  "/script.js",
  "/favicon.ico",
  "/food-fusilli-pasta-reginette-svgrepo-com.svg",
  "https://unpkg.com/html5-qrcode@2.3.8/html5-qrcode.min.js",
];

// РЈСЃС‚Р°РЅРѕРІРєР° СЃРµСЂРІРёСЃ-РІРѕСЂРєРµСЂР° Рё РєСЌС€РёСЂРѕРІР°РЅРёРµ С„Р°Р№Р»РѕРІ
self.addEventListener("install", (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => {
      return cache.addAll(urlsToCache);
    }),
  );
});

// РћР±СЂР°Р±РѕС‚РєР° Р·Р°РїСЂРѕСЃРѕРІ: СЃРЅР°С‡Р°Р»Р° СЃРµС‚СЊ, РїРѕС‚РѕРј РєСЌС€ (СЃС‚СЂР°С‚РµРіРёСЏ Stale-While-Revalidate)
self.addEventListener("fetch", (event) => {
  // РџСЂРѕРїСѓСЃРєР°РµРј Р·Р°РїСЂРѕСЃС‹ Рє API Рё РІРЅРµС€РЅРёРј СЂРµСЃСѓСЂСЃР°Рј, РєРѕС‚РѕСЂС‹Рµ РЅРµ РЅСѓР¶РЅРѕ РєСЌС€РёСЂРѕРІР°С‚СЊ Р°РіСЂРµСЃСЃРёРІРЅРѕ
  if (event.request.url.includes("openfoodfacts.org")) {
    // Р”Р»СЏ API РёСЃРїРѕР»СЊР·СѓРµРј С‚РѕР»СЊРєРѕ СЃРµС‚СЊ
    event.respondWith(fetch(event.request));
    return;
  }

  event.respondWith(
    caches.match(event.request).then((cachedResponse) => {
      const fetchPromise = fetch(event.request)
        .then((networkResponse) => {
          // РћР±РЅРѕРІР»СЏРµРј РєСЌС€ РЅРѕРІС‹Рј РѕС‚РІРµС‚РѕРј
          if (networkResponse && networkResponse.status === 200) {
            caches.open(CACHE_NAME).then((cache) => {
              cache.put(event.request, networkResponse.clone());
            });
          }
          return networkResponse;
        })
        .catch(() => {
          // Р•СЃР»Рё СЃРµС‚СЊ РЅРµРґРѕСЃС‚СѓРїРЅР° Рё РµСЃС‚СЊ РєСЌС€, РІРѕР·РІСЂР°С‰Р°РµРј РµРіРѕ
          if (cachedResponse) {
            return cachedResponse;
          }
          // Р•СЃР»Рё РЅРёС‡РµРіРѕ РЅРµС‚, РІРѕР·РІСЂР°С‰Р°РµРј РѕС„Р»Р°Р№РЅ-СЃС‚СЂР°РЅРёС†Сѓ (РѕРїС†РёРѕРЅР°Р»СЊРЅРѕ)
          return caches.match("/index.html");
        });

      // Р’РѕР·РІСЂР°С‰Р°РµРј РєСЌС€ РЅРµРјРµРґР»РµРЅРЅРѕ, РЅРѕ РѕР±РЅРѕРІР»СЏРµРј РІ С„РѕРЅРµ
      return cachedResponse || fetchPromise;
    }),
  );
});

// РђРєС‚РёРІР°С†РёСЏ Рё РѕС‡РёСЃС‚РєР° СЃС‚Р°СЂС‹С… РєСЌС€РµР№
self.addEventListener("activate", (event) => {
  const cacheWhitelist = [CACHE_NAME];
  event.waitUntil(
    caches.keys().then((cacheNames) => {
      return Promise.all(
        cacheNames.map((cacheName) => {
          if (cacheWhitelist.indexOf(cacheName) === -1) {
            return caches.delete(cacheName);
          }
        }),
      );
    }),
  );
});
